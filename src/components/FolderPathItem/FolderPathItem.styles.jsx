import styled from 'styled-components';
import { Link } from 'react-router-dom';

export const FolderPathItemContainer = styled(Link)`
  font-size: 15px;
`;

export const ArrowRightIcon = styled.span`
  padding: 0 13px;
`;
