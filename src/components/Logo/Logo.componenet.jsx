import React from 'react';
import { LogoContainer, LogoHead, LogoWrapper } from './Logo.styles';

const Logo = () => {
  return (
    <LogoWrapper to='/disk/my-disk' className='logo'>
      <LogoHead>chu</LogoHead>disk
    </LogoWrapper>
  );
};

export default Logo;
